server:
  port: ${SERVER_PORT:8888}

spring:
  application:
    name: gateway-svc
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:localhost}:${REGISTER_PORT:8848}

nacos:
  gateway:
    route:
      config:
        data-id: gateway-router
        group: DEFAULT_GROUP